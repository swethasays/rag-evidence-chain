"""
api/

FastAPI backend for the RAG Evidence Chain system.

Endpoints:
    GET  /health     — liveness check
    GET  /contracts  — list available contracts
    POST /ask        — run the RAG pipeline
"""